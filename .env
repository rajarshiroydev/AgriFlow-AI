# Environment variables
# SYNGENTA_AI_AGENT/.env.example
ENVIRONMENT="development"

# For Docker Compose internal networking and local script access if db is on localhost
DATABASE_URL="postgresql://syngenta_user:syngenta_password@postgres_db:5432/syngenta_supplychain_db"
# For running scripts LOCALLY against a Dockerized Postgres, use:
# DATABASE_URL_LOCAL="postgresql://syngenta_user:syngenta_password@localhost:5432/syngenta_supplychain_db"

CELERY_BROKER_URL="amqp://guest:guest@rabbitmq:5672//"
# For running scripts LOCALLY against a Dockerized RabbitMQ, use:
# CELERY_BROKER_URL_LOCAL="amqp://guest:guest@localhost:5672//"

CELERY_RESULT_BACKEND="redis://redis:6379/0"
# For running scripts LOCALLY against a Dockerized Redis, use:
# CELERY_RESULT_BACKEND_LOCAL="redis://localhost:6379/0"

# Add your LLM API Keys here
#OPENAI_API_KEY="sk-..." # For GPT-4o (if you use it directly)
ANTHROPIC_API_KEY="syn-6d5d18c8-cdea-47de-b94e-81039fbb18fc" # For Claude models

# Other settings
APP_BASE_URL="http://localhost:8000"
VECTOR_STORE_PATH="data/processed/vector_store"